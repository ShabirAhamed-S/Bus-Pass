<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buspass">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ican1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ican1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserLogin"></activity>
        <activity android:name=".userpass" />
        <activity android:name=".Activity_forget" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Home" />
        <activity android:name=".Login" />
        <activity android:name=".Activity2NewUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>